<UserControl x:Class="Fiddler.LocalRedirect.View.LocalRedirect"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:doc="http://schemas.microsoft.com/xps/2005/06/documentstructure"
             mc:Ignorable="d"              
             d:DesignHeight="300" d:DesignWidth="300" AllowDrop="True">
    <Grid>
        <Grid.RowDefinitions>
            <!-- TODO Need a toolbar row with export, import, add and delete buttons. -->
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0" ItemsSource="{Binding Redirects}" CanUserAddRows="True" CanUserDeleteRows="True" AutoGenerateColumns="False" Margin="0,0,-257,-39" Grid.RowSpan="2" AllowDrop="True" >
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="E" Binding="{Binding IsEnabled}" Width="Auto"/>
                <DataGridTextColumn Header="From url" Binding="{Binding FromUrl}" Width="2*"/>
                <DataGridTextColumn Header="To host" Binding="{Binding ToHost}" Width="*"/>
                <DataGridTextColumn Header="To port" Binding="{Binding ToPort}" Width="Auto"/>
                <DataGridTextColumn Header="Min" Binding="{Binding UseMinified}" Width="Auto"/>
            </DataGrid.Columns>
            <!--<DataGrid.RowDetailsTemplate>
                TODO Buttons to connect script here and set browserlink here, use minified etc.
                     Need a way to dif
                <
            </DataGrid.RowDetailsTemplate>-->
        </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock><Hyperlink>Add</Hyperlink></TextBlock>
            <TextBlock><Hyperlink>Remove</Hyperlink></TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
